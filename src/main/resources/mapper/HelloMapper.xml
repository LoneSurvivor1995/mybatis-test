<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//com.order.test.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mybatistest.mapper.HelloMapper">

    <select id="get" resultType="User" parameterType="int">
        SELECT * FROM t_user_00 t WHERE t.id=#{id}
    </select>

</mapper>